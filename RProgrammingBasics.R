#R Programming Basics 

factorial(4) #Function to find the value of factorial of 4
?factorial # Opens the help page of the function
factorial #Examine the code that function runs

sqrt(144) #Compute the square root of 144

Sys.Date() #Returns current date
Sys.info() #Returns system information
Sys.time() #Returns current date and time
Sys.timezone() #Returns timezone of the stystem.

print("test")


print(seq(32,44)) # Create a sequence of numbers from 32 to 44.


print(mean(25:82)) # Find mean of numbers from 25 to 82.

print(sum(41:68)) # Find sum of numbers from 41 to 68.

